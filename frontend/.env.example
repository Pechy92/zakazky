# Frontend API base URL
# Local dev usually uses Vite proxy, so this can stay empty.
# For Railway production set this to backend public URL.
# Example: VITE_API_URL=https://zakazky-backend-production.up.railway.app
VITE_API_URL=
